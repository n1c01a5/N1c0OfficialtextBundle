<h1>Create Officialtext Form</h1>
{%  if (form is not null) %}
    <form action="{{ url('api_1_post_officialtext') }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
{% endif %}
