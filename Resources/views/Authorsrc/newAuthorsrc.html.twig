<h1>Create Authorsrc Form</h1>
{%  if (form is not null) %}
    <form action="{{ url('api_1_post_officialtext_authorsrc', {'id': id}) }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
{% endif %}
